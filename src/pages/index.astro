---
const tasks = [
  "Hacer la práctica de astro",
  "Correr docker node:apline",
  "Subir la actividad a github",
  "Estudiar para el examen",
  "Hacer ejercicio"
];
---
<html lang="es">
<meta charset="UTF-8">
  <head>
    <title>Mis tareas pendientes. Víctor Monge</title>
    <style>
      /* Estilos para el botón */
      .big-red-button {
        background-color: blue;
        color: white;
        font-size: 20px;
        padding: 15px 30px;
        border: none;
        cursor: pointer;
        border-radius: 10px;
        margin-top: 20px;
      }
      .big-red-button:hover {
        background-color: darkred;
      }
      /* Estilos para el mensaje */
      .message {
        margin-top: 20px;
        font-size: 24px;
        color: green;
      }
    </style>
  </head>
  <body>
    <h1>Mis tareas pendientes. Víctor Monge</h1>
    <ul>
      {tasks.map((task) => (
        <li>{task}</li>
      ))}
    </ul>

    <!-- Botón rojo -->
    <button class="big-red-button" id="helloButton">Haz clic aqui</button>

    <!-- Mensaje "Hola, has presionado el boton" -->
    <div id="messageContainer" class="message" style="display: none;">
      ¡Hola, has presionado el boton!
    </div>

    <!-- Script para manejar el clic del botón -->
    <script>
      const button = document.getElementById("helloButton");
      const messageContainer = document.getElementById("messageContainer");

      button.addEventListener("click", () => {
        messageContainer.style.display = "block"; // Muestra el mensaje
      });
    </script>
  </body>
</html>